site_name: MIT-TP Research Scripts

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [.]   # so it can find driver/, preprocessor/

nav:
  - Home: index.md
  - Driver: api/driver.md
  - Preprocessor: api/preprocessor.md